class Solution:
    def islandPerimeter(self, grid):
        """
        :type grid: List[List[int]]
        :rtype: int
        """
        def check(x,y):
            if x == 0 :
                if y == 0:
                    

            
        for i, c in enumerate(grid):
            for j , v in enumerate(c):
                count = 0
                if v == 1:
                    


        